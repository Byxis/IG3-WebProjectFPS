/**
 * Enum representing the different phases of a match
 */
export enum MatchPhase {
  WAITING_FOR_PLAYERS = "WAITING_FOR_PLAYERS",
  WARMUP = "WARMUP",
  GAMEPLAY = "GAMEPLAY",
  RESULTS = "RESULTS",
}
